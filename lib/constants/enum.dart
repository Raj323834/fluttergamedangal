enum LeaderBoardItemType {
  score,
  about,
  tnc,
}

enum ShareIconType {
  telegram,
  whatsapp,
  messages,
  share,
}

enum GameType {
  sportsGame,
  cardGame,
  casualGame,
}

enum AddressProofType {
  select,
  passport,
  aadhaar,
  voterId,
  drivingLicense,
}

enum CashType {
  withdraw,
  deposit,
  gameChip,
  bonus,
  pokerChip,
  freeChip,
}
